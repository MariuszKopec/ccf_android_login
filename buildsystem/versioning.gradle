def MAJOR_VERSION="1"
def MINOR_VERSION="6"
def BUILD_NUMBER="0"

ext.getCcfVersion = {
    if (System.getenv('TRAVIS_BUILD_NUMBER') != null) {
        BUILD_NUMBER = System.getenv('TRAVIS_BUILD_NUMBER').toString()
    }
    return MAJOR_VERSION +'.'+ MINOR_VERSION +'.'+ BUILD_NUMBER
}
